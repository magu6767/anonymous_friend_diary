<% provide(:button_text, "投稿する") %>
<div class="row justify-content-center py-4">
  <div class="col-md-8">
    <div class="form-container p-4">
      <h2 class="text-center mb-4">新規投稿</h2>
      <%= form_with(model: @post) do |f| %>
        <%= render "shared/error_messages", object: f.object %>  
        <div class="mb-4">
          <%= f.label :title, "タイトル", class: "form-label" %>
          <%= f.text_field :title, class: "form-control" %>
        </div>

        <div class="mb-4">
          <%= f.label :content, "本文", class: "form-label" %>
          <%= f.text_area :content, rows: 8, class: "form-control" %>
        </div>

        <p class="text-muted mb-4">
          公序良俗に反する投稿はお控えください。投稿すると
          <%= link_to "利用規約", terms_path, target: "_blank" %>
          に同意したとみなされます。
        </p>
        <% unless logged_in? %>
          <div class="alert alert-info mt-3" role="alert">
            <%= link_to "ユーザー登録", signup_path %>をすると、あなたの投稿を気に入ったユーザーからリクエストを受け取れるようになります
          </div>
        <% end %>
        <%= f.submit yield(:button_text), class: "btn btn-primary" %>

      <% end %>
    </div>
  </div>
</div>
