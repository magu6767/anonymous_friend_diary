    <header>
        <nav class="navbar navbar-expand-lg navbar-light">
            <div class="container">
                <a class="navbar-brand" href="<%= root_path %>">匿名フレンドダイアリー</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="<%= help_path %>">使い方</a>
                        </li>
                        <% if logged_in? %>
                            <% if current_user.admin? %>
                                <li class="nav-item">
                                    <a class="nav-link" href="<%= users_path %>">ユーザー一覧</a>
                                </li>
                            <% end %>
                            <li class="nav-item">
                                <a class="nav-link btn btn-new-post" href="<%= new_post_path %>">新規投稿</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= friend_requests_path %>">リクエスト</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= friendships_path %>">フレンド</a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    アカウント
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li><a class="dropdown-item" href="<%= user_path(current_user) %>">プロフィール</a></li>
                                    <li><a class="dropdown-item" href="<%= edit_user_path(current_user) %>">設定</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" href="<%= logout_path %>" data-method="delete">ログアウト</a></li>
                                </ul>
                            </li>
                        <% else %>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= login_path %>">ログイン</a>
                            </li>
                        <% end %>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>