    <header>
        <nav class="navbar navbar-expand-lg navbar-light">
            <div class="container">
                <a class="navbar-brand" href="<%= root_path %>">匿名フレンド</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <% if logged_in? %>
                            <% if current_user.admin? %>
                                <li class="nav-item">
                                    <a class="nav-link" href="<%= users_path %>">ユーザー一覧</a>
                                </li>
                            <% end %>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= new_post_path %>">投稿する</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= friend_requests_path %>">リクエスト</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= friendships_path %>">フレンド</a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    アカウント
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li><a class="dropdown-item" href="<%= user_path(current_user) %>">プロフィール</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                      <%= form_with url: logout_path, method: :delete do %>
                                        <%= submit_tag "ログアウト", class: "dropdown-item", data: { confirm: "本当にログアウトしますか？" } %>
                                      <% end %>
                                    </li>                                
                                </ul>
                            </li>
                        <% else %>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= new_post_path %>">投稿する</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= login_path %>">ログイン</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= signup_path %>">新規登録</a>
                            </li>
                        <% end %>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <%# <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script> %>