<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= render "layouts/rails_default" %>
  </head>
  <body>
    <%= render "layouts/header" %>  <!-- Header -->
    <%= yield %>
    <%= render "layouts/footer" %>  <!-- Footer -->
    <%= debug(params) if Rails.env.development? %>  <!-- Debugging information -->
  </body>
</html>
